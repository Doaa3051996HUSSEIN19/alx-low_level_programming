#include "main.h"
/**
*_islower _check for lower cases
*@c the character to be checked
*Return: 1 if true 0 if enter anything else
**/

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	
	return (1);
	}
	else
        return (0);
}
